---

- name: Remove VLANs
  bigip_vlan:
      name: "{{ item }}"
      server: "{{ inventory_hostname }}"
      server_port: "{{ bigip_port }}"
      user: "{{ bigip_username }}"
      password: "{{ bigip_password }}"
      state: "absent"
  register: result
  with_items:
      - net1
      - net2