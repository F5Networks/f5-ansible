---
- name: Activate (upload, install, reboot) base image
  bigip_software2:
      software: "{{ iso_base }}"
      state: "activated"
      volume: "{{ volume_new }}"
  register: result

- name: Assert Activate (upload, install, reboot) base image
  assert:
      that:
          - result|changed