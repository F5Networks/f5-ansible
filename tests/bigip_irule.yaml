---

# Test the bigip_irule module
#
# This playbook can be used to test the bigip_irule module.
#
# Usage:
#
#    ansible-playbook -i inventory/hosts tests/bigip_irule.yaml [OPTIONS]:
#
# Examples:
#
#    Run all of the tests
#
#    ansible-playbook -i inventory/hosts test/bigip_irule.yaml
#

- name: Test the bigip_irule module
  hosts: f5-test
  connection: local

  roles:
      - __bigip_irule