---

# Test the bigip_user module
#
# This playbook can be used to test the bigip_user module.
#
# Usage:
#
#    ansible-playbook -i inventory/hosts tests/bigip_user.yaml [OPTIONS]:
#
# Examples:
#
#    Run all of the tests
#
#    ansible-playbook -i inventory/hosts test/bigip_user.yaml
#

- name: Test the bigip_user module
  hosts: f5-test
  connection: local

  roles:
      - __bigip_user