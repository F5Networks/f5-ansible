---

# Test the bigip_pool module
#
# This playbook can be used to test the bigip_pool module.
#
# Usage:
#
#    ansible-playbook -i inventory/hosts tests/bigip_pool.yaml [OPTIONS]:
#
# Examples:
#
#    Run all of the tests
#
#    ansible-playbook -i inventory/hosts test/bigip_pool.yaml
#

- name: Test the bigip_pool module
  hosts: f5-test
  connection: local

  roles:
      - __bigip_pool