---

- nodeset:
    name: ubuntu-latest
    nodes:
      - name: controller
        label: ubuntu-latest

- nodeset:
    name: centos-7
    nodes:
      - name: controller
        label: centos-7

- nodeset:
    name: centos-latest
    nodes:
      - name: controller
        label: centos-latest

- nodeset:
    name: fedora-latest
    nodes:
      - name: controller
        label: fedora-latest

- nodeset:
    name: fedora-28
    nodes:
      - name: controller
        label: fedora-28
