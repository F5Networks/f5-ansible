
- job:
    name: base
    parent: base-minimal
    abstract: true
    description: |
      The base job for the F5-Ansible installation of Zuul.
    pre-run: .zuul.d/playbooks/base/pre.yaml

- job:
    name: plain-run
    parent: base
    description: |
      Just a basic job to see zuul do something.
    run: .zuul.d/playbooks/job-playbook.yaml
